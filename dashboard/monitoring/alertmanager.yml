route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'void-pirate-notifications'

receivers:
  - name: 'void-pirate-notifications'
    webhook_configs:
      - url: 'http://192.168.0.39:8080/api/alerts/router'
        send_resolved: true
