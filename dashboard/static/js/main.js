// VOID Pirate Trading Co — Captain Dashboard main.js
// SPA entry point — delegates to tab-specific inline scripts
console.log('⚓ VOID Pirate Captain Dashboard loaded');
document.addEventListener('DOMContentLoaded', function() {
    // SPA navigation handled by inline scripts in each tab
    // This file serves as the bundle entry point for any future build step
});
